<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ium.pg.warehouseclient">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.findbyid.FindByIdActivity" />
        <activity android:name=".activity.deletebyid.DeleteByIdActivity" />
        <activity android:name=".activity.add.AddActivity" />
        <activity android:name=".activity.modify.ModifyActivity" />
        <activity android:name=".activity.changequantity.ChangeQuantityActivity" />
        <activity android:name=".activity.warehousemanagement.WarehouseManagementActivity" />
    </application>

</manifest>